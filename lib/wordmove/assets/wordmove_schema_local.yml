type: map
mapping:
  vhost:
    pattern: /^https?:\/\//
  wordpress_path:
  database:
    type: map
    required: true
    mapping:
      name:
        required: true
      user:
        required: true
      password:
        required: true
      host:
        required: true
      socket:
      mysql_options:
      mysqldump_options:
      port:
  paths:
    type: map
    mapping:
      wp_content:
      wp_config:
      uploads:
      plugins:
      mu_plugins:
      themes:
      languages:
